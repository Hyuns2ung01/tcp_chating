<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <title>
        <%= post.title %>
    </title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body {
            background-color: #f0f2f5;
            font-family: 'Apple SD Gothic Neo', 'Noto Sans KR', sans-serif;
            margin: 0;
            padding-bottom: 100px;
            /* 아래쪽 여백 */
        }

        /*공통 컨테이너 스타일 (게시글, 댓글 둘 다 적용) */
        .container-box {
            width: 800px;
            background: #fff;
            padding: 50px;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
            text-align: left;

            /* 핵심: 화면 중앙 정렬 및 위아래 간격 */
            margin: 40px auto;
        }

        /* 게시글 헤더 */
        .post-header {
            border-bottom: 1px solid #eee;
            padding-bottom: 20px;
            margin-bottom: 30px;
        }

        .badge-cat {
            display: inline-block;
            padding: 5px 12px;
            border-radius: 15px;
            font-size: 0.85em;
            font-weight: bold;
            color: white;
            margin-bottom: 15px;
        }

        .cat-used {
            background-color: #FF9800;
        }

        .cat-buy {
            background-color: #2196F3;
        }

        .cat-hi {
            background-color: #4CAF50;
        }

        .cat-notice {
            background-color: #D32F2F;
        }

        .cat-free {
            background-color: #9E9E9E;
        }

        h1 {
            margin: 0;
            font-size: 2em;
            color: #333;
            line-height: 1.3;
            letter-spacing: -0.5px;
        }

        .meta-info {
            display: flex;
            align-items: center;
            gap: 15px;
            margin-top: 15px;
            color: #777;
            font-size: 0.95em;
        }

        .meta-item {
            display: flex;
            align-items: center;
            gap: 6px;
        }

        /* 본문 */
        .content {
            min-height: 200px;
            font-size: 1.1em;
            line-height: 1.8;
            color: #333;
            text-align: left;
            white-space: pre-line;
            word-break: break-all;
            margin-bottom: 50px;
        }

        /* 버튼 바 */
        .action-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-top: 1px solid #f5f5f5;
            padding-top: 25px;
        }

        .btn-group-right {
            display: flex;
            gap: 10px;
        }

        .btn {
            border: none;
            padding: 10px 20px;
            border-radius: 12px;
            font-size: 0.95em;
            font-weight: 600;
            cursor: pointer;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            transition: all 0.2s;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        .btn-back {
            background-color: #f1f3f5;
            color: #495057;
        }

        .btn-back:hover {
            background-color: #e9ecef;
        }

        .btn-edit {
            background-color: #2196F3;
            color: white;
        }

        .btn-edit:hover {
            background-color: #1e88e5;
        }

        .btn-del {
            background-color: #ff5252;
            color: white;
        }

        .btn-del:hover {
            background-color: #ff1744;
        }

        .btn-chat {
            background-color: #FF9800;
            color: white;
        }

        .btn-chat:hover {
            background-color: #fb8c00;
        }

        /* 댓글 스타일 */
        .comment-header {
            margin-top: 0;
            border-bottom: 2px solid #f0f2f5;
            padding-bottom: 15px;
            font-size: 1.2em;
            color: #333;
        }

        .comment-item {
            border-bottom: 1px solid #f0f2f5;
            padding: 20px 0;
        }

        .comment-item:last-child {
            border-bottom: none;
        }

        .cmt-meta {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 8px;
        }

        .cmt-author {
            font-weight: bold;
            color: #333;
            font-size: 1em;
        }

        .cmt-date {
            font-size: 0.8em;
            color: #aaa;
            margin-left: 10px;
        }

        .cmt-content {
            white-space: pre-line;
            color: #555;
            line-height: 1.6;
            font-size: 0.95em;
        }

        .btn-cmt-del {
            background: none;
            border: none;
            color: #ccc;
            cursor: pointer;
            font-size: 0.9em;
            transition: 0.2s;
        }

        .btn-cmt-del:hover {
            color: #ff5252;
        }

        .comment-form {
            display: flex;
            gap: 10px;
            margin-bottom: 20px;
            background: #f9f9f9;
            padding: 15px;
            border-radius: 15px;
        }

        .comment-form textarea {
            flex: 1;
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 10px;
            resize: none;
            height: 50px;
            outline: none;
            font-family: inherit;
        }

        .comment-form textarea:focus {
            border-color: #4CAF50;
            background: #fff;
        }

        .btn-cmt-submit {
            background-color: #4CAF50;
            color: white;
            border: none;
            padding: 0 25px;
            border-radius: 10px;
            cursor: pointer;
            font-weight: bold;
        }

        .btn-cmt-submit:hover {
            background-color: #43a047;
        }
    </style>
</head>

<body>

    <div class="container-box">
        <div class="post-header">
            <% let badgeClass='cat-free' ; if (post.category==='중고거래' ) badgeClass='cat-used' ; else if
                (post.category==='공구' ) badgeClass='cat-buy' ; else if (post.category==='가입인사' ) badgeClass='cat-hi' ;
                else if (post.category==='공지' ) badgeClass='cat-notice' ; %>
                <span class="badge-cat <%= badgeClass %>">
                    <%= post.category %>
                </span>
                <h1>
                    <%= post.title %>
                </h1>
                <div class="meta-info">
                    <div class="meta-item"><i class="fa-solid fa-user"></i> <span>
                            <%= post.author_name || '탈퇴한 유저' %>
                        </span></div>
                    <div class="meta-item"><i class="fa-solid fa-eye"></i> <span>
                            <%= post.view_count %>
                        </span></div>
                </div>
        </div>

        <div class="content">
            <%= post.content %>
        </div>

        <div class="action-bar">
            <a href="/posts" class="btn btn-back"><i class="fa-solid fa-list"></i> 목록으로</a>
            <div class="btn-group-right">
                <% if (user && user.id !==post.author_id && (post.category==='중고거래' || post.category==='공구' )) { %>
                    <form action="/chat/start" method="POST" style="margin:0;">
                        <input type="hidden" name="seller_id" value="<%= post.author_id %>">
                        <input type="hidden" name="post_title" value="<%= post.title %>">
                        <input type="hidden" name="post_id" value="<%= post.id %>">
                        <input type="hidden" name="category" value="<%= post.category %>">

                        <button type="submit" class="btn btn-chat">
                            <% if (post.category==='공구' ) { %>
                                <i class="fa-solid fa-users"></i> 공구 참여
                                <% } else { %>
                                    <i class="fa-solid fa-comments"></i> 1:1 채팅
                                    <% } %>
                        </button>
                    </form>
                    <% } %>

                        <% if (user && (user.id===post.author_id || user.is_admin)) { %>
                            <a href="/posts/<%= post.id %>/edit" class="btn btn-edit"><i class="fa-solid fa-pen"></i>
                                수정</a>
                            <form action="/posts/<%= post.id %>/delete" method="POST" style="margin:0;"
                                onsubmit="return confirm('정말 삭제하시겠습니까?');">
                                <button type="submit" class="btn btn-del"><i class="fa-solid fa-trash"></i> 삭제</button>
                            </form>
                            <% } %>
            </div>
        </div>
    </div>

    <div class="container-box">
        <h3 class="comment-header">💬 댓글 (<%= comments.length %>)</h3>

        <% if (user) { %>
            <form action="/posts/<%= post.id %>/comments" method="POST" class="comment-form">
                <textarea name="content" placeholder="댓글을 남겨보세요..." required></textarea>
                <button type="submit" class="btn-cmt-submit">등록</button>
            </form>
            <% } else { %>
                <div style="text-align: center; padding: 30px; background: #f9f9f9; border-radius: 10px; color: #666;">
                    댓글을 작성하려면 <a href="/login" style="color: #4CAF50; font-weight: bold;">로그인</a>이 필요합니다.
                </div>
                <% } %>

                    <ul style="list-style: none; padding: 0; margin: 0;">
                        <% if (comments.length===0) { %>
                            <li style="text-align: center; color: #999; padding: 40px 0;">
                                <i class="fa-regular fa-comment-dots"
                                    style="font-size: 2em; margin-bottom: 10px;"></i><br>
                                아직 댓글이 없습니다. 첫 댓글을 남겨보세요!
                            </li>
                            <% } else { %>
                                <% comments.forEach(cmt=> { %>
                                    <li class="comment-item">
                                        <div class="cmt-meta">
                                            <div>
                                                <span class="cmt-author">
                                                    <%= cmt.commenter_name %>
                                                </span>
                                                <span class="cmt-date">
                                                    <%= new Date(cmt.created_at).toLocaleString() %>
                                                </span>
                                            </div>

                                            <% if (user && (user.id===cmt.user_id || user.is_admin)) { %>
                                                <form action="/posts/<%= post.id %>/comments/<%= cmt.id %>/delete"
                                                    method="POST" onsubmit="return confirm('삭제하시겠습니까?');"
                                                    style="margin:0;">
                                                    <button type="submit" class="btn-cmt-del" title="삭제">
                                                        <i class="fa-solid fa-xmark"></i>
                                                    </button>
                                                </form>
                                                <% } %>
                                        </div>
                                        <div class="cmt-content">
                                            <%= cmt.content %>
                                        </div>
                                    </li>
                                    <% }) %>
                                        <% } %>
                    </ul>
    </div>

</body>

</html>