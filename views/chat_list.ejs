<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <title>채팅방 목록</title>
    <link rel="stylesheet" href="/style.css">
    <style>
        body {
            width: 600px;
            margin: 40px auto;
            font-family: 'Apple SD Gothic Neo', sans-serif;
            background-color: #f0f2f5;
        }

        .list-container {
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        h2 {
            border-bottom: 2px solid #ddd;
            padding-bottom: 10px;
            margin-top: 0;
        }

        /* 방 만들기 폼 */
        .create-form {
            display: flex;
            gap: 10px;
            margin-bottom: 20px;
        }

        .create-form input {
            flex: 1;
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 5px;
            outline: none;
        }

        .create-form button {
            padding: 10px 20px;
            background: #4CAF50;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
        }

        .create-form button:hover {
            background-color: #43a047;
        }

        /* 방 목록 리스트 */
        .room-list {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        /* [수정됨] 리스트 아이템 스타일 */
        .room-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 15px;
            border-bottom: 1px solid #eee;
            transition: all 0.2s;

            /* 마우스 올렸을 때 클릭 가능한 느낌 주기 */
            cursor: pointer;
            /* 더블클릭 할 때 글자가 드래그 선택되는 것 방지 */
            user-select: none;
        }

        /* 마우스 올렸을 때 배경색 변경 */
        .room-item:hover {
            background-color: #e8f5e9;
            /* 연한 초록색 */
            transform: translateX(5px);
            /* 살짝 오른쪽으로 이동 효과 */
        }

        .room-link {
            text-decoration: none;
            color: #333;
            font-weight: bold;
            font-size: 1.1em;
            flex: 1;
        }

        /* 안내 문구 스타일 */
        .hint-text {
            font-size: 0.8em;
            color: #888;
            margin-left: 10px;
        }

        .back-link {
            text-decoration: none;
            color: #666;
        }

        .back-link:hover {
            color: #333;
            font-weight: bold;
        }
    </style>
</head>

<body>
    <div class="list-container">
        <h2>💬 채팅방 대기실</h2>

        <form action="/chat/create" method="POST" class="create-form">
            <input type="text" name="title" placeholder="새로운 채팅방 제목..." required autocomplete="off">
            <button type="submit">+ 방 만들기</button>
        </form>

        <ul class="room-list">
            <% if (rooms.length===0) { %>
                <li style="text-align: center; padding: 40px; color: #888;">
                    개설된 채팅방이 없습니다.<br>새로운 방을 만들어보세요!
                </li>
                <% } else { %>
                    <% rooms.forEach(room=> { %>
                        <li class="room-item" ondblclick="window.location.href='/chat/<%= room.id %>'"
                            title="더블클릭하여 입장하기">

                            <span class="room-link">📢 <%= room.title %></span>
                        </li>
                        <% }) %>
                            <% } %>
        </ul>

        <div style="margin-top: 25px; text-align: right;">
            <a href="/posts" class="back-link">🏠 홈으로 돌아가기</a>
        </div>
    </div>
</body>

</html>